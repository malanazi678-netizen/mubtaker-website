# تعليمات رفع وتشغيل موقع مبتكر

هذه الحزمة تحتوي على مشروع **Full-stack** مبني بـ React وVite وExpress وtRPC. لذلك تحتاج الاستضافة إلى تشغيل تطبيق Node.js، وليست استضافة ملفات ثابتة فقط. كما تحتاج قاعدة بيانات MySQL أو TiDB حتى تعمل المصادقة ومساحة الملفات.

## التشغيل على استضافة Node.js

1. ارفع محتويات هذا المجلد إلى خادم يدعم Node.js.
2. استخدم Node.js إصدار 20 أو أحدث، ثم شغّل `pnpm install --frozen-lockfile` أو `npm install`.
3. أنشئ متغيرات البيئة المطلوبة في لوحة الاستضافة. لا تضع القيم السرية داخل المستودع أو داخل ملفات الواجهة.
4. شغّل أمر البناء: `pnpm build`.
5. شغّل التطبيق بأمر: `node dist/index.js`.
6. اربط النطاق بعملية Node.js، واترك المنفذ من متغير البيئة الذي توفره الاستضافة؛ التطبيق لا يفترض منفذاً ثابتاً.

## المتغيرات المطلوبة

| المتغير | الاستخدام |
|---|---|
| `DATABASE_URL` | رابط قاعدة MySQL أو TiDB للمستخدمين وسجلات الملفات |
| `JWT_SECRET` | توقيع جلسات تسجيل الدخول |
| `VITE_APP_ID` | معرّف تطبيق OAuth |
| `OAUTH_SERVER_URL` | خادم OAuth |
| `VITE_OAUTH_PORTAL_URL` | بوابة تسجيل الدخول في الواجهة |
| `OWNER_OPEN_ID` و`OWNER_NAME` | بيانات مالك المشروع |
| `BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY` | خدمات التخزين المدمجة والعمليات الخلفية |
| `VITE_FRONTEND_FORGE_API_URL` و`VITE_FRONTEND_FORGE_API_KEY` | إعدادات خدمات الواجهة عند الحاجة |

## الأصول

تم تضمين الصور المستخدمة داخل `client/public/assets`، وتم تحويل مساراتها في نسخة التسليم إلى `/assets/...` حتى تعمل على الاستضافة الخارجية. لا تحذف هذا المجلد.

## ملاحظة مهمة عن التخزين وتسجيل الدخول

إذا كانت الاستضافة من نوع cPanel أو Static Hosting ولا تدعم Node.js وقاعدة بيانات واتصالات OAuth، فلن تعمل المصادقة ومساحة الملفات والرفع الخلفي بمجرد رفع الملفات. في هذه الحالة يلزم استخدام استضافة Node.js مُدارة أو تشغيل الواجهة فقط بعد فصل ميزات الخادم.

## التحقق المحلي قبل الرفع

```bash
pnpm install --frozen-lockfile
pnpm check
pnpm test
pnpm build
node dist/index.js
```

لا ترفع ملف `.env` الحقيقي أو أي مفاتيح سرية ضمن ZIP.
